package com.itshaharcha.assessment.entity;

/** The four interaction primitives; every IELTS question type reduces to one of these. */
public enum ProblemType {
    INPUT,
    RADIO,
    SELECT,
    MULTI_SELECT
}
